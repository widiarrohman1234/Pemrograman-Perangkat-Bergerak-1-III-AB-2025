import 'package:flutter/material.dart';

class HelloWorldFlutter extends StatelessWidget {
  const HelloWorldFlutter({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("Hello World")]);
  }
}
